Library of Drm features, including oemcrypt api for DRM L1.

This library is pre-built by MediaTek internal.

WHAT IT DOES?
=============
liboemcrypto will be used by drm, including mtk in-house tee & trustonic tee.
Without them, classic drm L1 will not work.

HOW IT WAS BUILT?
==================
It needs the following library from AOSP:
1. libcrypto.so
2. libcutils.so
3. libdl.so
4. liblog.so
5. libstlport.so
6. libutils.so
7. libz.so

and the following libs from MediaTek:

All source/dependency modules of this module are already put in
'system/lib' folder.

HOW TO USE IT?
==============
liboemcrypto provides interface of drm L1, it is built library.
